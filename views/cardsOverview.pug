extends layout
block content 
  h2 My Sets
    //Check if there are any flash card sets
    if flashCardSets.length > 0
        console.log(flashCardSets)
        console.log(flashCardSet)
        ul
            each set in flashCardSets
                li
                    h2 #{set.name}
                    p #{set.topic}
    else
        p You have no flash card sets.
